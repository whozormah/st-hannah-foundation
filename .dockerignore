# Keep the build context small and the image clean. node_modules is
# reinstalled inside the image from the committed lockfile, and .next is
# rebuilt there — copying either in would defeat both.
node_modules
.next
.git
.github
.env
.env.*
!.env.example
docs
tests
*.log
.DS_Store
nginx/certs
